# Copy to .env and fill in. Never commit .env.
# LLM provider key (whichever we choose)
LLM_API_KEY=
# Dataset source: local folder or the scorer server URL
INBOX_SOURCE=data
# INBOX_SOURCE=http://localhost:8080
